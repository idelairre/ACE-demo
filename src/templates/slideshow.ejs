<header>
    <div id="close-btn">
        <a id="close-btn" href="#">x</a>
    </div>
</header>
<div id="cbp-fwslider" class="cbp-fwslider">
    <ul>
      <% slides.forEach(function (slide) { %>
        <li><img src="<%= slide %>" alt="<%= id %>"/></li>
      <% }) %>
    </ul>
</div>
