  <% decks.forEach(function (deck) { %>
    <li class="mdl-list__item">
        <span class="mdl-list__item-primary-content menu-deck" data-deck="<%= deck %>">
            <%= deck %>
        </span>
    </li>
  <% }) %>
